# ---- Web framework ----
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.20
sse-starlette==3.4.8

# ---- Config / validation ----
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.0.1

# ---- Database ----
SQLAlchemy[asyncio]==2.0.52
alembic==1.19.1
asyncpg==0.31.0
aiosqlite==0.22.1
greenlet==3.1.1

# ---- Cache / idempotency ----
redis==8.1.0

# ---- Payments (TEST MODE ONLY) ----
razorpay==2.0.1

# ---- Agent interoperability (MCP server) ----
mcp==2.1.1

# ---- Agent orchestration ----
langgraph==1.2.11
anthropic==1.2.0

# ---- HTTP ----
httpx==0.28.1

# ---- Testing ----
pytest==9.1.1
pytest-asyncio==1.4.0
# Pinned to what a clean resolve actually produces alongside mcp 2.x.
# 4.7.0 was pinned here for months and was uninstallable from scratch: the
# local venv only worked because installing mcp had upgraded anyio in place,
# so the file described an environment that could not be recreated.
anyio==4.14.2
